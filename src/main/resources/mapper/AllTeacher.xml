<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.alex.demo.springtest.mapper.AllTeacherMapper">
<!--
    &lt;!&ndash; 通用查询映射结果 &ndash;&gt;
    <resultMap id="BaseResultMap"
               type="com.alex.demo.springtest.entity">
        <id column="class_id" property="classId" />
        <result column="teacher_id" property="teacherId" />
        <result column="teacher_name" property="teacherName" />
    </resultMap>
    &lt;!&ndash; 通用查询结果列 &ndash;&gt;
&lt;!&ndash;    <sql id="Base_Column_List">

    </sql>&ndash;&gt;

    <select id="findByClassId" parameterType="java.lang.Integer"
            resultType="com.alex.demo.springtest.entity">
            SELECT b.class_id,c.teacher_id,c.teacher_name
            FROM contect_ct a
            INNER JOIN class b
            INNER JOIN teacher c
            ON a.class_id = b.class_id
            AND a.teacher_id = c.teacher_id
            WHERE b.class_id = #{classId};
    </select>-->


</mapper>